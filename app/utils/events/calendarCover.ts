// app\utils\events\calendarCover.ts
// Fallback artwork for /calendario cards/detail view when an Event or
// Tournament has no cover image of its own (user-supplied, 2026-08-15 —
// self-hosted under public/ rather than hotlinked from its original source).
export const DEFAULT_CALENDAR_COVER_IMAGE = '/calendar-default-cover.webp'
