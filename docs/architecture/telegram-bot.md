# Bot Telegram

<!-- docs/architecture/telegram-bot.md -->

Compagno leggibile dell'implementazione (`server/utils/telegram/`, `server/api/telegram/webhook.post.ts`) — stesso schema di `docs/architecture/permissions.md`: qui lo stato di ogni funzionalità, non solo l'intenzione. Il bot è **@PauperwaveBot**. Per il dettaglio "chi viene notificato per quale evento" vedi `docs/architecture/telegram-notifications.md`.

Ogni comando vive nel proprio file sotto `commands/`, registrato tramite `commands/index.ts` (barrel) — `bot.ts` chiama solo `registerCommands(bot)`. `commands/index.ts` tiene l'unica `CommandGroup` condivisa (`@grammyjs/commands`): ogni `register*Command` chiama `commands.command(nome, descrizione, handler)`, e `commands.setCommands(bot)` sincronizza da lì il picker "/" di Telegram — un solo punto da tenere aggiornato quando un comando cambia nome/descrizione.

Ogni schermata con bottoni e navigazione (`/calendario`, `/leghe`, `/iscrizioni`, `/eventi`, `/prossimo`, `tournament/detail.ts`'s `torneoMenu`) usa `@grammyjs/menu`: istanze `Menu` con `.dynamic()`/`.submenu()`/`.back()` invece di `InlineKeyboard` + `bot.callbackQuery(/regex/)` manuale. `torneoMenu` è condiviso da più menu radice (calendario, leghe, iscrizioni, prossimo) — per tornare esattamente alla pagina di provenienza (non a una vista radice generica), il bottone "indietro" non usa `back()`/`nav()` nativi del plugin (che non possono portare un payload fresco al menu di destinazione) ma una gestione manuale condivisa — `server/utils/telegram/menuNav.ts`'s `navigateBack`/`registerMenu`/`getMenu`. Ogni `Menu` disabilita `onMenuOutdated` (`false`): l'euristica di staleness del plugin (fingerprint su righe/bottoni ricostruito rileggendo `.dynamic()` ad ogni pressione) genera falsi positivi ("Menu was outdated, try again!") perché ogni `.dynamic()` qui rilegge dati live da Supabase — ogni handler valida comunque da sé la freschezza dei dati (es. "Torneo non trovato"), rendendo il controllo del plugin ridondante.

## Legend

- 🟢 Implementato e funzionante
- 🟡 Parziale — funziona ma con una limitazione nota (vedi Note)
- 🔴 Pianificato, non ancora costruito
- ⚫ Fuori scope per ora — richiede una feature nuova nell'app stessa, non solo nel bot

## Livelli di accesso

- **Pubblico** — chiunque scriva al bot, nessun collegamento richiesto
- **Collegato** — richiede aver associato la chat a un socio (email fornita al primo `/start`, verificata su `pauperwave_associates`)

## Comandi e funzionalità

| Comando/funzionalità | Accesso | Stato | Note |
|---|---|---|---|
| `/start` | Pubblico | 🟡 | Risponde con un messaggio di benvenuto. Non chiede ancora l'email di collegamento (vedi riga sotto). |
| `/help` | Pubblico | 🟢 | Elenco comandi disponibili, raggruppato per dominio (`commands/core.ts`). |
| `/status` | Pubblico | 🟢 | Liveness check, risponde "🟢 Bot operativo." con commit e data del deploy corrente. |
| `/classifiche` | Pubblico | 🟡 | Pauper/Commander/Premodern: calcolo reale (riusa `groupBestNByPlayer`, `shared/utils/cittadino/bestNStandings.ts`), top 10 + link alla pagina completa. Cittadino: solo link, nessun calcolo (scoring best-11 diverso, non replicato nel bot — vedi Note). |
| `/eventi` | Pubblico | 🟢 | Prossimi 8 eventi `published`/`ongoing`, letti direttamente da `events` via client anon (`commands/eventi.ts`). Ogni evento apre un dettaglio (data completa, location con link maps, organizzatore, immagine se assegnata — foto con caption come `/calendario`), con 🧭 Direzioni e 🗓️ Aggiungi al calendario. Nessuna azione di iscrizione — `EventParticipants.vue` è ancora un placeholder, gli eventi non hanno un flusso di partecipazione self-service come i tornei. |
| `/calendario` | Pubblico | 🟢 | Tornei del mese raggruppati per giorno, con numero di tappa (`Nª tappa`) per i tornei di lega — calcolato su tutta la storia della lega, non solo sui tornei aperti/futuri che il bot interroga di suo (`fetchStageNumbers`). Esclude sempre i tornei di negozi esterni (Magman etc., status `external`). Ogni torneo ha il proprio bottone (`torneo:<uuid>:<origin>`, `origin` = `m<meseOffset>` o `l<indiceLega>` a seconda di dove si è entrati nel dettaglio) che apre una vista dettaglio (data/ora, location con link maps, organizzatore, referente, quota, premi, descrizione, immagine se assegnata — inviata come foto con caption, max 1024 caratteri). Il bottone di iscrizione riflette lo stato reale (➕ Iscriviti / ❌ Annulla iscrizione / 🎯 Check-in effettuato — quest'ultimo non annullabile, spiega perché al tocco), più due bottoni utility sempre presenti: 🧭 Direzioni e 🗓️ Aggiungi al calendario (Google Calendar `render` URL, nessun backend proprio). Nessuna sessione lato server, tutto lo stato nel `callback_data`. |
| `/leghe` | Pubblico | 🟢 | Leghe `status = active`, con progresso tornei (`completati/totali`, cancellati esclusi dal denominatore) e intervallo date (`commands/leghe.ts`). Ogni lega ha un bottone (`lega:<indice>`, indice = posizione nell'elenco leghe attive ordinate per `starts_at`) che apre l'elenco dei suoi tornei (tutti gli stati, non solo quelli aperti). Riusa la stessa vista dettaglio di `/calendario` (`torneo:<uuid>:l<indice>`), con "« Torna alla lega" al posto di "« Torna al mese". Ogni bottone mostra lo stato di iscrizione dell'utente collegato a quella specifica tappa (✅ iscritto, 🎯 check-in fatto, 🎲 non iscritto). |
| `/prossimo` | Pubblico | 🟢 | Solo il prossimo torneo `registration_open`/`in_progress` in ordine cronologico (`commands/prossimo.ts`). Riusa la stessa vista dettaglio di `/calendario` (`torneo:<uuid>:p`), incluse le azioni di iscrizione. |
| Collegamento chat↔socio | Collegato | 🟢 | `commands/linking.ts`: nessuno stato di conversazione (Nitro su Vercel è serverless) — un `bot.on('message:text')` catch-all, registrato per ultimo dopo ogni comando, tratta **qualunque** messaggio testuale che assomiglia a un'email come un tentativo di collegamento, verifica su `pauperwave_associates` (`membership_request_status = 'approved'`), poi scrive in `pauperwave_associate_telegram_links` (cancella prima l'eventuale riga esistente per quel `chat_id`, poi upsert su `associate_uuid` — gestisce sia il caso "stessa chat, nuova email" sia "stesso socio, nuovo device"). `/start` invita a scrivere l'email. **Rate-limit sui tentativi falliti ancora da aggiungere** (email da sola è indovinabile/nota, accettato per ora — vedi Note in `telegram-notifications.md`). |
| `/collegamento` | Pubblico | 🟢 | `commands/collegamento.ts`. Sola lettura: verifica se la chat corrente è già collegata (`resolveAssociateUuidByChatId`) e, se sì, mostra nome e email del socio collegato — utile per controllare/confermare il collegamento senza dover indovinare se un comando personale (`/tessera`, `/iscrizioni`) funzionerà. Distinto dal collegamento vero e proprio (riga sopra). |
| `/tessera` | Collegato | 🟢 | `commands/tessera.ts`. Legge `pauperwave_associates_with_status` (non la tabella base — è la view che calcola `membership_status`/`latest_renewal_*` dalle renewal), mostra stato (attivo/da rinnovare/scaduto/quota non pagata/...), numero socio, data e anno dell'ultimo rinnovo. |
| `/iscrizioni` | Collegato | 🟢 | `commands/iscrizioni.ts`. Elenca i tornei `registration_open`/`in_progress` a cui l'associato collegato è iscritto (✅ iscritto, 🎯 check-in fatto), col numero di tappa per i tornei di lega. Bottone per ognuno che riusa la vista dettaglio di `/calendario`. |
| `/supporto` | Pubblico | 🟢 | `commands/supporto.ts`. Flusso a due passi senza stato lato server: il comando risponde con un prompt in `ForceReply`, e Telegram garantisce che il messaggio successivo dell'utente sia una reply a quel prompt esatto — un `bot.on('message:text')` dedicato riconosce la richiesta di supporto confrontando `reply_to_message.text`. Inoltrato ai soli `super_admin` via `sendTelegramMessage` + `get_admin_telegram_chat_ids({ p_roles: ['super_admin'] })`, con l'username Telegram del mittente e se è un socio collegato. |
| Iscriversi/disiscriversi a un torneo | Collegato | 🟢 | `self-register.post.ts`/`self-unregister.post.ts` (ownership-checked, RLS `player_own_registration`/`player_delete_own`), collegati al bottone "Iscriviti"/"Disiscriviti" su `/calendario` e `/calendar` (`RegisterButton.vue`). Il bot chiama la stessa RPC `register_tournament_players` direttamente (service-role, non l'endpoint HTTP — nessuna sessione Supabase da passare fuori da una request), risolvendo l'associato dal `chat_id` collegato. |
| Alert admin — nuova domanda di tesseramento, richiesta di rinnovo | — (staff, ruolo `admin`/`super_admin`) | 🟢 | `associates/apply.post.ts` e `associates/renew.post.ts` chiamano `notifyTelegramAdmins()` dopo `recordMembershipEvent(...)`. Best-effort: un errore Telegram/DB viene loggato, non fa fallire la richiesta di tesseramento/rinnovo (`notify.ts`, `Promise.allSettled`). Destinatari risolti da `get_admin_telegram_chat_ids()` (funzione Postgres), niente `TELEGRAM_ADMIN_CHAT_ID` da env var. |
| Alert errori tecnici | — (solo `super_admin`) | 🟡 | `notifyTelegramSuperAdmins()` esiste (`notify.ts`, stessa RPC di sopra con `p_roles: ['super_admin']`), ma nessun errore dell'app la chiama ancora. |
| Promemoria torneo (giorno prima + un'ora prima) | Collegato | 🔴 | Usa iscrizione + data torneo, nessun pairing necessario. Proposto: job `pg_cron` (stesso pattern di `purge_expired_trash()`, ADR-028) che ogni 15-30 min interroga le iscrizioni in finestra e chiama un endpoint Nitro dedicato. Non ancora un comando. |
| Tavolo/avversario per turno (`/tavolo`) | Collegato | ⚫ | `tournament_pairings`/`tournament_rounds` esistono nel DB, ma nessun `server/api/*` scrive su quelle tabelle durante un torneo in corso — sembrano popolate solo via backfill storico. Finché non esiste un flusso di scrittura live, il bot non ha un "tavolo di adesso" da poter leggere. Le tabelle sono inoltre modellate specificamente per Commander (fino a 4 giocatori per pairing), da rivedere per Pauper/Premodern (Swiss 1v1). Stub registrato in `commands/stubs.ts`, risponde spiegando il blocco. Tracciato in [issue #82](https://github.com/Pauperwave/app/issues/82), insieme alle due righe sotto. |
| Votare miglior mazzo/miglior giocata (`/vota`) | Collegato | ⚫ | Stesso blocco della riga sopra — `tournament_votes` esiste ma è inutilizzata, e il voto è legato a un `pairing_uuid` reale che non esiste finché non c'è un flusso di pairing live. |
| Inserire il comandante giocato in un turno | Collegato | ⚫ | Stesso blocco — andrebbe scritto su `tournament_round_results.commander_deck_uuid`, ma quella riga esiste solo se il turno è già stato gestito da un organizer. |

## Note

**La sicurezza del collegamento chat↔socio è deliberatamente leggera.** L'opzione più sicura ("Collega Telegram" dalla sezione Profilo del sito, con token monouso generato da una sessione già autenticata) è stata scartata — il giocatore medio non visita mai le Impostazioni del sito, quel flusso non verrebbe mai usato. Si è scelto invece di chiedere solo l'email direttamente in chat al primo `/start`, accettando che l'email da sola sia un fattore debole (nota/indovinabile), perché i dati dietro il collegamento oggi sono a bassa sensibilità (stato tesseramento, preferenze di notifica — non saldo, non pagamenti). Se in futuro il bot esporrà dati più sensibili, l'asticella di verifica va alzata solo per *quel* comando specifico, non per il collegamento base.

**`/classifiche` non calcola Cittadino perché il suo scoring è strutturalmente diverso** (miglior-11 su tutti i formati, con tie-break — ADR-012, `docs/PROGRESS.md`) da quello delle classifiche per formato (`groupBestNByPlayer` semplice). Replicarlo nel bot avrebbe richiesto portare anche quella logica lato server prima che i dati sottostanti siano reali (issue #2) — rimandato.

**TODO: possibile traduzione EN/DE in futuro.** Ogni comando ha oggi le sue stringhe italiane hardcoded direttamente nel proprio `commands/*.ts`, nessun uso del sistema i18n dell'app (pensato per la UI Vue/Nuxt lato client — il bot gira come singleton Nitro fuori da quel contesto). Se in futuro serve inglese/tedesco andrà introdotto un meccanismo di i18n lato server dedicato al bot, non riuso diretto del sistema esistente.

## Aggiornare questa tabella

Ogni volta che un comando passa da 🔴/⚫ a 🟡/🟢 (o viceversa, se qualcosa si rompe/viene rimosso) o viene eliminato, aggiornare/rimuovere la riga qui — stessa disciplina di `docs/architecture/permissions.md`: la tabella deve riflettere lo stato reale del codice, non l'intenzione né la sua storia (per quella, `docs/PROGRESS.md`/`CHANGELOG.md`).
