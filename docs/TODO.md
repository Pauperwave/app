# TODO

Loose observations and open questions, not yet committed to the backlog. Promote to `docs/BACKLOG.md` (with priority and effort estimate) once scoped.

- **Verify RLS/permissions for `pauperwave_associate_renewals` actually match what the app sees.** `pauperwave_associates_with_status` (added 2026-08-05) uses `security_invoker = true`, so its `LEFT JOIN` to `pauperwave_associate_renewals` is filtered by the *querying user's* RLS, not a superuser's. The table has no blanket `authenticated` policy — only `player_own_renewals` (own record only) and `management_full_access` (`has_management_permissions(auth.uid())`). All verification of the view's `active`/`to_renew` counts so far was done via `supabase db query --linked`, which runs as a privileged role and bypasses RLS entirely. Never confirmed whether the actual admin user's `auth.uid()` satisfies `has_management_permissions` — if it doesn't, the app sees `latest_renewal_year = NULL` for every associate and everyone falls into `expired`, silently wrong.
  - Found: 2026-08-05, while reasoning about the new membership-status view's RLS exposure.
  - Next step: as the logged-in admin, check what `pauperwave_associates_with_status` actually returns for `membership_status`/`latest_renewal_year`, compared to the privileged CLI query.

- **Full audit of all 28 `public` schema tables for the same class of issues found on `pauperwave_associates`.** This session found, on a single table: a dead always-`NULL` column (`associate_status`, dropped), a frontend/DB field name drift (`request_status` vs `membership_request_status`), several wrong nullability assumptions in the hand-written TypeScript type, and (documented separately in `docs/BACKLOG.md` P1) an overly-permissive catch-all RLS policy. Since this DB is meant to become the new base for `MagicTheGathering/league`, the same checks (dead/unused columns, DB-vs-code field name drift, nullability, RLS policy overlap) should be run systematically across the other 26 tables before treating the schema as stable.
  - Found: 2026-08-05, after fixing the above on `pauperwave_associates` alone and realizing it was found reactively (via a user-reported bug), not systematically.
  - Next step: table-by-table pass — columns + constraints + RLS policies vs actual app usage, starting with the other tables in the `pauperwave_*` family (`pauperwave_payments`, `payment_receipts`) since they're closest in spirit to what already got audited.
  - Scope note: `app` is meant to become a full gestionale for PauperWave — multi-format tournament organization (not just Commander: also Premodern, Draft, Pauper, etc.), membership renewals, payments, and tournament creation/management. The schema mixes Commander-specific tables (`mtg_commanders`, `commander_decks`, `tournament_kills`, `rulesets`/`ruleset__points`/`ruleset__descriptions` — points-based multiplayer-pod scoring is a Commander concept) with format-agnostic ones (`tournaments`, `tournament_registrations`, `tournament_standings`, `mtg_formats`). While auditing, flag anywhere the schema or app code implicitly assumes "it's Commander" instead of treating format as first-class data — those assumptions won't hold once Premodern/Draft/Pauper need the same tables.

- **Sidebar "Commander" section is hardcoded/static, not format-driven.** Added 2026-08-06 alongside stub pages for `/players`, `/commanders` (catalog), `/rulesets` — genuinely missing routes compared to `MagicTheGathering/league`'s route list. Labeled the section "Commander" explicitly (not folded into "Competizioni") to be honest that these are format-specific, not generic.
  - Found: 2026-08-06, while reasoning about how the sidebar should represent multiple formats going forward.
  - Why not generalized now: `mtg_formats` has 0 rows — no format, including Commander itself, is actually recorded as data yet. Building a dynamic per-format sidebar (e.g. reading `mtg_formats` to render a section per format) now would be speculative architecture for formats that don't exist in the DB yet — the same class of premature work already deferred elsewhere (ADR-003).
  - Next step: revisit when a second real format (Premodern/Draft/Pauper) gets actual DB rows and its own UI — that's the point to design format-aware navigation instead of a static "Commander" label.

- **`/tournaments` and `/leagues` grid+table views are still mock-backed, not real.** 2026-08-09: rebuilt both pages (event card grid + `UTable`, status filter, `ViewModeTabs`) as a design exploration, explicitly against `server/api/tournaments.ts`/`leagues.ts` mock data — the same category of "UI theater" the 727-line tournaments table was torn out for in `b542714` (2026-08-06). Kept deliberately this time (per user decision) to validate the grid-card direction, but it is throwaway: `Tournament`/`League` types, `useTournamentsQuery`/`useLeaguesQuery`, and both `GridView.vue` components all assume the mock's shape (snake_case rows, `entry_fee`, `registered_players`, `completed_tournament_count` — the last has no real column anywhere).
  - Found: 2026-08-06 (original), reaffirmed 2026-08-09 when the same mistake was about to repeat.
  - Next step: once real `tournaments`/`tournament_registrations`/`leagues` tables exist in Supabase, rebuild `useTournamentsQuery`/`useLeaguesQuery` against them (not `/api/tournaments`/`/api/leagues`), keep the grid/table UI shape if it still holds up, and delete the mock endpoints. Includes the "numero di iscritti"/"formato"/"quota di iscrizione" columns (`tournament.columns.registeredPlayers`, `.format`, `.entryFee`) already covered by mock data — verify against the real schema too.
  - **Same applies to `/cittadino` (added 2026-08-09, at user request, as a layout mockup).** `server/api/cittadino.ts` generates a seeded players × events matrix across three editions; `useCittadinoQuery.ts` computes points, best-11 selection and the tie-break ordering from raw placements. The *aggregation* is written against the real regulation and should survive; the *endpoint* is throwaway and must be replaced once `cittadino_editions` + `tournaments.cittadino_edition_uuid` carry real data (see the P1 entry in `docs/BACKLOG.md`). Note the query composable still uses `useAsyncData`, not the Pinia Colada pattern — deliberate for a mockup, but it is one more caller to migrate under the P1 data-layer item.

- **Tournament detail stepper needs dynamic per-step status once real round-tracking data exists.** `MagicTheGathering/league`'s `TournamentStepper.vue` gives each step a `description` computed from actual tournament state (registration open/viewing, round completed/in-progress/pending/viewing/correcting, tournament ended) and lets you click a past step to preview it (with snap-back for non-navigable ones). PauperWave's `tournaments/[tournamentId]/index.vue` adopted only the visual title+description shape, with static placeholder descriptions ("In attesa", "Iscrizioni aperte") — there's no `currentRound`/`tournamentStatus` concept backing it yet.
  - Found: 2026-08-06, comparing our tournament stepper against league's while deciding which pattern to adopt.
  - Next step: once tournaments have real round-tracking data (current round, per-round completion), mirror league's `roundStepDescription()` logic and add click-to-preview navigation for past rounds.

- ~~**Direction reversed 2026-08-12: `/wanted-cards`' table now needs to catch up to `app.config.ts`'s new default, not the other way around.**~~ Resolved 2026-08-13: `/wanted-cards/index.vue`'s `UTable` no longer has any `:ui` override at all — its former `td: 'empty:p-0'` (added to collapse TanStack grouping's empty spacer row) was promoted into `app.config.ts`'s own `ui.table.slots.td` (now `empty:p-0 empty:border-0`, every grouped table needs it, confirmed via `/transactions` having the same bug). Visually confirmed in-browser across several sessions of active use — the inherited `tr` hover does not clash with row-grouping/context-menu interactions.

- **Simplify `/associates` table column headers.** Current headers are raw/verbose (e.g. full field labels for every one of the ~35 columns); needs a pass to shorten/clarify.
  - Found: 2026-08-07, user request.
  - Next step: scoping — decide which headers to shorten and to what, likely alongside the styling unification above since both touch the same table.

- ~~**Add a contextual (right-click) row menu to `/associates`' table.**~~ Done 2026-08-12: `useAssociatesRowActions.ts` (mirrors `useWantedCardsRowActions.ts`'s `rowContextMenuItems`/`onRowContextmenu`/`tableContextMenuItems` shape) + `UContextMenu` wrapping `UTable` on both `associates/index.vue` and `requests.vue`. Items: "Modifica" (always, opens `AssociatesListEditModal.vue`, backed by a new `server/api/associates/[id]/update.post.ts` BFF endpoint) and "Approva" (only when `membership_request_status === 'pending'`, reuses the existing `approveAssociates` mutation). No "Elimina" — not requested, no delete endpoint exists for associates.

- **No status for a member who was active but stopped renewing.** `membership_status` (`useAssociatesTableColumns.ts`'s badge config, `associatesStatusCounts` in `associates/index.vue`) only has two values: `active` | `to_renew`. There's no third state for "was a member, didn't renew, now lapsed/inactive" — someone who's been `to_renew` for years looks the same in the UI as someone who renewed last week and is `to_renew` for the upcoming cycle.
  - Found: 2026-08-12, user question while reviewing the associates edit-modal work.
  - Two shapes discussed, neither decided: (1) a real third `membership_status` value (`expired`/`inactive`), computed from `to_renew` + payment/association date past some grace period, or set explicitly — likely needs a DB migration since `membership_status` comes from the `pauperwave_associates_with_status` view; (2) keep the two-value column as-is and treat `to_renew` past a cutoff as implicitly lapsed only in the UI (badge color change, no new value, no migration).
  - Next step: needs a business decision first — how long is "still renewable" vs. "lapsed" (grace period length), who/what flips the state (automatic on a cutoff date vs. an explicit staff action), and whether a lapsed member should keep showing in the roster at all vs. move to its own view (same shape as the pending/rejected split `associates/requests.vue` already did for request status). Scope before touching schema or `useAssociatesTableColumns.ts`.

- **"Elimina" on a wanted-card request is back in the UI without a frontend admin check.** Initially removed entirely (2026-08-07) since there's no role/permission check wired into the frontend anywhere yet (checked: no `useSupabaseUser`-based role check, no admin flag on `Associate`, `settings/MembersList.vue`'s role dropdown isn't tied to auth) — reinstated same day at user request (with a confirmation modal) relying solely on the DB-level RLS policy (`has_management_permissions(auth.uid())`, same as update) to reject non-admins; a non-admin sees an error toast instead of a silently-ignored click.
  - Found: 2026-08-07, user request (initial removal), reversed same day (user request).
  - Next step: once there's a real way to know "is the current logged-in user an admin" client-side, hide/disable "Elimina" for non-admins instead of letting the request round-trip and fail.

- **`/wanted-cards` is really the seller's view, not a generic search — rethink the UI around that.** User insight (2026-08-07): almost nobody browsing the *wanted* list would search by card name — that's what a *seller* does ("does anyone want this card I have?"). The card-name search input has been hidden from the toolbar for now (still wired in state/filtering, just no UI control) since its current placement/framing doesn't match that use case.
  - Found: 2026-08-07, user observation during a mobile-toolbar UX discussion.
  - Next step: design a dedicated, prominent search entry point for sellers (separate from the list-browsing filters). **Update 2026-08-08:** the "own requests" half of this is now done — "Le mie richieste" filter button resolves the logged-in user to a `pauperwave_associates` row via email match (`currentAssociate` in `wanted-cards/index.vue`) and filters to only their cards. Only the seller-facing search entry point remains open.

- **Zero test files anywhere in the app, `/wanted-cards` included, despite `vitest`/`Playwright` being configured.** The runner is wired up (`pnpm test`/`test:e2e`, mirroring `league`) but no `*.test.*` file exists — every verification of `/wanted-cards` this session (CRUD, filters, tour, context menu, audit columns) was done manually via browser. Felt acutely on this route given how much surface it now has (two views, filters, mutations, permissions). See `docs/architecture/testing.md`.
  - Found: 2026-08-08, self-assessment of `/wanted-cards` maturity at user request.
  - Next step: `useWantedCardsFilters.ts`'s `filteredCards` (the single predicate now shared by both views, see `docs/architecture/testing.md`) is the highest-value first test given it already caused one drift bug historically.

- **Non-admin path on `/wanted-cards` never verified end-to-end.** We know insert is open to any authenticated user, and update/status/delete require `has_management_permissions` (enforced server-side in the BFF endpoints, see ADR-007/ADR-008). But no one has actually logged in as a non-management user and confirmed: (a) they can create a request, (b) attempting edit/delete/status-change surfaces a readable error toast instead of a confusing failure.
  - Found: 2026-08-08, self-assessment of `/wanted-cards` maturity at user request.
  - Next step: test with a non-admin associate account once one exists/is identifiable.

- **Mobile not re-tested since the tour/spotlight/filter additions.** Known issue (not prioritized for now, per user 2026-08-08): the guided tour's `#tour-wanted-cards-first-card` step assumes grid view is active — if a user starts the tour while on table view, that step's target won't exist in the DOM. Broader mobile responsiveness (toolbar wrapping with the new "Le mie richieste" button, tour popover sizing on small screens) also hasn't been checked since the last mobile pass earlier in the wanted-cards work.
  - Found: 2026-08-08, self-assessment of `/wanted-cards` maturity at user request.
  - Next step: low priority per user — revisit if/when mobile usage of this route actually matters for the 2026-08-30 deadline.

- **Pagination vs. virtualization/infinite scroll for `/wanted-cards`.** User preference (2026-08-08): no pagination — prefers virtualization + infinite scroll, for both the table and the card grid, once dataset size actually warrants it (currently 47 rows, a non-issue).
  - Found: 2026-08-08, self-assessment of `/wanted-cards` maturity at user request.
  - Next step: revisit once row count grows enough to matter; look at TanStack Virtual (already a `@tanstack/vue-table` sibling package) for both views.

- **`app/components/magic/` depends on a `wanted-cards` type.** `magic/CardPreview.vue` is typed on `ScryfallPrinting`, which lives in `app/composables/useScryfallCardSearch.ts` — so the generic MTG component folder points back at a specific domain's composable. Accepted knowingly when the component was moved out of `wanted-cards/` (2026-08-08) to make it available to the Commander work: the alternative was leaving it behind and duplicating it later.
  - Found: 2026-08-08, while moving `ManaCost`/`CardPreview`/`CardPreviewTooltip` into `app/components/magic/` so the Commander section can reuse them.
  - Next step: when Commander actually consumes `MagicCardPreview`, either move `ScryfallPrinting` into `app/types/index.d.ts` (it's shared domain data, which is where the convention in `CLAUDE.md` says it belongs) or narrow the component's prop to the handful of fields it renders. Don't do it speculatively — the second consumer is what tells us which of the two is right.

- **4 components with no references anywhere.** `tournaments/single/{Participants,RoundResults}.vue`, `leagues/single/Leaderboard.vue`, `rounds/single/ResultAddModal.vue` — the `rounds/` folder exists solely for the last one.
  - Found: 2026-08-08, while auditing component folders for domain leakage (which turned up only `HomeDateRangePicker`, see below).
  - Resolved part of it: these are deliberate stubs, not leftovers from the Nuxt UI template — none exists in that template, each has its own translated i18n key, and all are named after PauperWave domain concepts. The two `Overview.vue` stubs (`tournaments/single/` and `tournaments/list/`) were deleted 2026-08-09 at the user's request along with their i18n keys, since the tournament detail page is a stepper with no overview step and the list page is a grid/table.
  - Next step: finish or delete the remaining four. `leagues/single/Leaderboard.vue` is the most likely to be revived — it is closely related to the Campionato Cittadino standings (see `docs/BACKLOG.md` P1) and may share its rendering.

- **`HomeDateRangePicker` is not a home component.** Used by 7 pages, only one of which is `pages/index.vue`; the rest are statistics, transactions, events, leagues and tournaments. Same shape as `ViewModeTabs` before it was moved to `app/components/` root.
  - Found: 2026-08-08, auditing component folders after moving `PlayerTag`/`ManaCost`/`CardPreview*` out of `wanted-cards/`.
  - Next step: move to `app/components/DateRangePicker.vue` (`<DateRangePicker>`), updating the 7 call sites. Cheap, but touches pages unrelated to whatever else is in flight — worth doing on its own.

- *(Promoted 2026-08-09 to `docs/BACKLOG.md` P1: the "Campionato Cittadino" cross-tournament ranking. The open question that kept it here — league vs. orthogonal axis — was settled by the user in favour of orthogonal, and the official regulation supplied the scoring rules.)*

- **`/events` public page idea: card calendar view with downloadable ICS.** The `eventi.pauperwave.org` subdomain (see `settings/domains.vue`) is meant to expose a static page showing events as a calendar of cards, each with an ICS download so a visitor can add it straight to their phone's calendar. Not scoped or scheduled — just the shape of the idea, noted 2026-08-09.
  - Next step: needs its own public route (excluded from auth, same checklist as the cittadino/tesseramento subdomains — see `docs/BACKLOG.md` P1) and an ICS generator for the `events`/`tournaments` data before this goes anywhere.

- **Per-format standings pages/subdomains: Commander, Premodern, Pauper.** Same idea as the cittadino ranking but scoped to a single format instead of cross-format — `commander.pauperwave.org`, `premodern.pauperwave.org`, `pauper.pauperwave.org` (rows added to `settings/domains.vue` 2026-08-09, all `route: null` — nothing built). Explicitly **static tables**, not live queries: refreshed only when an event is marked as ended ("termina evento"), not the always-current view the cittadino standings imply. That's a different data-refresh model from every other public-page idea recorded so far and needs its own mechanism (a snapshot written on the "end event" action, not a query run on page load).
  - Next step: needs a "termina evento" action to actually exist first (checked 2026-08-09: no such action/endpoint currently exists — see `tournaments`/`events` status columns for what's there today), and a decision on where the per-format snapshot lives (a new table per format? one table keyed by format?) before any of the three pages can be built.

- **`DetailPageHeader` extraction deferred.** The `UDashboardNavbar` (leading collapse + right `NotificationsBellButton`) + `UDashboardToolbar`/`UBreadcrumb` shell repeats near-identically across `associate/[slug].vue`, `tournaments/[tournamentId]/index.vue`, `events/[eventId]/index.vue`, `leagues/[leagueId]/index.vue`, and their nested tournament sub-routes. Deliberately not extracted yet (user decision, 2026-08-10): most of these detail routes aren't reachable from anywhere else in the app yet (no links into them from the list pages), so their header shape is still likely to change once that navigation actually gets built. Revisit once those routes are wired up and the shell has stopped moving.

- **`/tesseramento` (public membership-application form, backs `tesseramento.pauperwave.org` per ADR-011) has two open items before it can go live.** Built 2026-08-11: `UStepper`-based public form (email OTP verification as the anti-abuse gate instead of a captcha, per user decision — proves control of a real inbox before `server/api/associates/apply.post.ts` is ever reachable), reusing `associateFormSchema` (shared with `AddModal.vue`'s internal form) and the same field sections.
  - The statute checkbox links externally to `blog.pauperwave.org/docs/statuto` (2026-08-11 decision — it's the association's own canonical governance document, not something this app should mirror, unlike GDPR consent text which is coupled to what this specific system does). `/tesseramento/informativa-dati` still renders placeholder text ("Testo da inserire") — need the association's real data-processing notice before publishing.
  - The video-surveillance consent/notice (`has_acknowledged_surveillance_notice`, `/tesseramento/informativa-sorveglianza`) was dropped entirely 2026-08-11: the association moved premises and it's no longer known whether the new location has video surveillance, so the notice stopped being accurate. Both forms (`AddModal.vue`, `/tesseramento`) no longer ask for it; the DB column stays (`NOT NULL`, has a default) for existing associates' historical records — shown read-only in `associates/index.vue`'s hidden-by-default column and `associate/[slug].vue`'s detail view, never collected as a new consent again.
  - The P1 in `docs/BACKLOG.md` on `pauperwave_associates`'s overly-permissive catch-all RLS policy is still open — ADR-011 says close it before publishing `tesseramento`/`eventi`, but the user explicitly deferred that here (2026-08-11): the new endpoint writes via `serverSupabaseServiceRole` like every other BFF endpoint, so it bypasses RLS regardless, but the policy still over-exposes `pauperwave_associates` to any authenticated user through every *other* client path. Close before the subdomain is actually wired up in DNS.
  - Also unresolved: how `tesseramento.pauperwave.org` actually reaches `/tesseramento` on this deploy — no hostname-based routing exists yet, this is an infra/reverse-proxy decision outside this app's code.

- **Component auto-import `pathPrefix` convention mismatch with `MagicTheGathering/league`.** PauperWave's `nuxt.config.ts` defaults `pathPrefix: true` (component tag includes its folder path, e.g. `AssociatesListApproveModal`) with `inputs/` as the sole `pathPrefix: false` exception. `league` sets `pathPrefix: false` globally — every component tag is just its filename regardless of nesting, and base filenames must stay unique across the whole tree. Found 2026-08-12 while comparing folder-organization conventions between the two repos (`league`'s `app/components/CLAUDE.md`).
  - Why it matters: ADR-003 in `docs/PROGRESS.md` targets integrating with `league` by 2026-08-30 — this repo's components will need to align with one convention or the other at that point (or `league`'s repo absorbs PauperWave's, or vice versa).
  - Next step: not worth reconciling now — revisit when the actual integration/merge work starts, and decide then which convention wins (or whether the merged app keeps both via per-folder `dirs` overrides, same mechanism already used for `inputs/`).

- **Membership fee rule (€5, always PayPal Friends&Family) is a hardcoded constant, not a Settings-editable value.** Added 2026-08-12 alongside the "Rinnova" associate action (`app/utils/associates/membershipFee.ts`'s `MEMBERSHIP_FEE_AMOUNT`/`MEMBERSHIP_FEE_PAYMENT_METHOD`, used by `TransactionsListAddModal.vue` to default `payment_amount`/`payment_method` whenever `payment_type` is "Association Fee"). It's an association bylaw rule (the regolamento), not derived from any table — currently the only place it's written down at all is that one file's comment.
  - Found: 2026-08-12, user request while reviewing the renewal flow — extracted to a named constant specifically so this is easy to find/change later, with an eye towards eventually making it Settings-editable instead of a code constant.
  - Next step: not scoped — would need a place to persist association-wide rules (a new table? a JSON column somewhere? `settings/` has no such storage today, only role/domain/notification config) and a Settings UI section for the regolamento's numeric rules. Revisit if/when other regolamento-derived values show up needing the same treatment (worth generalizing once there's a second one, not for a single constant).

- **Bulk selection (checkbox column + toolbar-swap actions bar) convention still needs to reach `/tournaments`, `/leagues`, `/events`, `/players` once each gets real row actions.** 2026-08-13: `useSelection<TId>()` (generalized from wanted-cards' own selection composable) + the "swap the toolbar's `#left`/`#right` for a bulk-actions bar while something is selected" pattern (`WantedCardsListBulkActionsBar`'s `side` prop, avoids the layout shift a separately-inserted row caused) is now used by `/wanted-cards` and `/associates/requests`. The other four routes were skipped for now: `/tournaments`, `/leagues`, `/events` are still mock-data-backed with no row actions at all (see the mock-backed entry above), and `/players` has no table/data source yet at all — there is nothing to bulk-act on.
  - Found: 2026-08-13, user request to roll bulk selection out to every route with (or planned to have) row actions.
  - Next step: when any of these four routes gets real data + a `UContextMenu` row-actions setup (mirroring `useAssociatesRowActions.ts`/`useWantedCardsRowActions.ts`), add the checkbox column + toolbar-swap bar as part of that same work, reusing `useSelection.ts` rather than a new per-domain Set.

- **`/transactions`: bulk-send payment receipts via Resend, possibly grouped by event.** 2026-08-13 user request, not scoped or started: integrate the Resend email service to send payment receipts, likely as a per-row button in the transactions table, with a bulk "send all receipts for this event" variant under consideration too. Needs its own design pass — a Resend API key/account, a receipt email template, and a decision on whether bulk-send is scoped by event, by date range, or by the existing bulk-selection checkboxes — before any implementation starts.
  - Found: 2026-08-13, raised while scoping bulk actions for `/transactions` (deferred there in favor of this instead).
  - Next step: scope with the user — template content, Resend account/API key setup, and the bulk-send trigger UX (per-event button vs. selection-driven).

- **`TournamentStatus`/`LeagueStatus`/`EventStatus` should become real Postgres enums, not just TypeScript unions.** Added 2026-08-10 alongside `Event`/`EventStatus` and mock `server/api/events.ts`. Right now all three (`app/types/index.d.ts`) are backed by mock JSON (`server/api/{tournaments,leagues,events}.ts`), so the enum only exists in TS — nothing stops a stray string reaching the "DB" once these move to real Supabase tables. Needs a `create type ... as enum (...)` migration per status column when each entity gets a real table (same treatment `app_role` already got, see `docs/architecture/roles.md`), plus a decision on whether the three enums should actually be one shared type (they're currently near-identical: `scheduled`/`ongoing`/`completed`, with `canceled` on `Tournament` and `Event` but not `League` — deliberate per 2026-08-10 discussion, a whole league season felt less "cancelable" in one step than a single event/tournament, but worth re-confirming once real data exists).
